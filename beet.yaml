id: transportduct
name: TransportDuct
description: transportduct
author: AirDox_

output: "build"

meta:
  mc_supports: ["1.20","1.20.1"]
  mc_supports_dataversion: [3463,3465]


data_pack:
  load: [.]
resource_pack:
  load: [.]

extend: ["common.yaml"]