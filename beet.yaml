id: transportduct
name: TransportDuct
version: "0.0.0"
description: transportduct
author: author

meta:
  supports: ["1.20","1.20.1"]

data_pack:
  load: [.]
resource_pack:
  load: [.]

extend: ["common.yaml"]